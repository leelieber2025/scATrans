[egg_info]
egg_base = .
# Write scatrans.egg-info at the project root during builds rather than under src/.
# This keeps the packaged source tree (src/scatrans) clean inside sdists
# and reduces accidental commits of generated metadata.
